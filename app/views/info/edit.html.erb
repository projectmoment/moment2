<h1>info 수정 페이지</h1>

<form action="/info/update/<%= @info.id%>" method="post" enctype="multipart/form-data">
    <div>
        <p>프로필 사진</p>
        
        <input type="file" name="image_url" id="info_image_url"><br>
    </div>

    <div>
        <p>자기소개</p>
        <input type="text" name="introduction">
    </div>
    
    <div class="aa">
        <p>하는 게임을 모두 선택하시오.</p>
        <% @categories.each do |category|%> 
            <%= category.name %>
            <% if current_user.following?(category) %>
                <%= link_to "On", unfollow_info_path(category.id), method: 'post', remote: true , class: "game#{category.id}" %><br>
            <% else %>
                <%= link_to "Off",follow_info_path(category.id), method: 'post', remote: true, class: "game#{category.id}" %><br>
            <% end %>
        <% end %>
    </div>
    
    <input type="submit" value="저장">
</form>
    
