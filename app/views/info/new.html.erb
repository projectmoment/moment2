<h1>info 생성 페이지</h1>
<% if @info.blank? %>
    <form action="/info/create" method="post" enctype="multipart/form-data">
        <div>
            <p>프로필 사진</p>
            <input type="file" name="image_url" id="info_image_url"><br>
        </div>
    
        <div>
            <p>자기소개</p>
            <input type="text" name="introduction">
        </div>
        
        <input type="submit" value="저장">
    </form>
<% else %>
    <div>
        <p>프로필 사진</p>
        <img src= "<%= @info.image_url%>" >
    </div>
        
    <div>
        <p>자기소개</p>
        <%= @info.introduction %>
    </div>
<% end %>


<div>
    <p>하는 게임을 모두 선택하시오.</p>
    <form action="/plays/create/<%= current_user.id %>" method="post">
        <% @categories.each do |category|%> 
            <p>
              <label>
                <input type="checkbox" name="category_id[]" value= "<%= category.id %>" class="filled-in"  />
                <span><%= category.name %></span>
              </label>
            </p>
        <% end %> 
        
        <input type="submit" value="저장">  
    </form>    
       
</div>

<div>
   <p>하는 게임을 모두 선택하시오.</p>
    <% @categories.each do |category|%> 
        <% if current_user.following?(category.id ) %>
        <%= link_to "Following", {action: 'unfollow', id: @user.id}, method: 'post', remote: true %>
        <% elsif current_user != @user %>
        <%= link_to "Follow", {action: 'follow', id: @user.id}, method: 'post', remote: true %>
        <% elsif current_user == @user %>
        여기선 이 문구가 뜨고 다른 유저 페이지에서는 Follow가 뜸 (기능구현완료) 일단 profile / mypage 에만 적용시켜둠
        <% end %>
    <% end %>
</div>











