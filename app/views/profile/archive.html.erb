<h4>[상단 네브바]</h4>

<div class="grid">
  <div class="grid-box a">프로필 사진</div>
  <div class="grid-box b">자기소개</div>
  <div class="grid-box c">
    <% if current_user.following?(@user) %>
    <%= link_to "Following", {action: 'unfollow', id: @user.id}, method: 'post', remote: true %>
    <% elsif current_user != @user %>
    <%= link_to "Follow", {action: 'follow', id: @user.id}, method: 'post', remote: true %>
    <% elsif current_user == @user %>
    여기선 이 문구가 뜨고 다른 유저 페이지에서는 Follow가 뜸
    <% end %>
  </div>
  <div class="grid-box d">설정</div>
  <div class="grid-box e">하는 게임</div>
</div>

<% if current_user != @user.id %>
  <ul class="tabs tabs-fixed-width tab-demo z-depth-1">
    <li class="tab"><a href="/profile/mypage/<%= @user.id %>">프로필 메인</a></li>
    <li class="tab"><a href="/profile/<%= @user.id %>/album">앨범</a></li>
    <li class="tab"><a href="/profile/<%= @user.id %>/archive">아카이브</a></li>
    <li class="tab"><a href="/profile/<%= @user.id %>/tagged">태그됨</a></li>
  </ul>
<% else %>
  <ul class="tabs tabs-fixed-width tab-demo z-depth-1">
    <li class="tab"><a href="/profile/mypage/<%= current_user.id %>">프로필 메인</a></li>
    <li class="tab"><a href="/profile/<%= current_user.id %>/album">앨범</a></li>
    <li class="tab"><a href="/profile/<%= current_user.id %>/archive">아카이브</a></li>
    <li class="tab"><a href="/profile/<%= current_user.id %>/tagged">태그됨</a></li>
  </ul>
<% end %>

<h6>profile/user_id/archive. 해당 유저가 작성한 모든 board 아카이브로 표시. 여기서 글쓰기 가능</h6>

<div class="box-container">
    <div class="row">
        <% @board.each do |board| %>
            <div class="col m4 l4">
                <div class="box">
                <a href="/board/<%= board.id %>">
                  <img class="image" src="<%= board.image_url %>"></a>
                    <div class="padding">
                        <div class="">
                            <% if user_signed_in? && current_user.id == @user.id %>
                            <a href="/board/edit/<%= board.id %>"><i class="material-icons">edit</i></a>
                            <%= link_to '삭제', board, method: :delete, data: { confirm: '진짜 삭제?' } %>
                            <% end %>
                            <b><%= board.profile.user.name %></b>
                            <%= board.title %>
                            <%= board.content %>
                        </div>
                    </div>
                </div>
            </div>
        <% end %>
    </div>
</div>

<a href="/">홈으로</a>

<!-- CSS -->

 <style>
  body {
    background-color: #FAFAFA;
  }
  
  a:hover {
    background: none;
  }
  
  .box-container {
    text-align: center;
  }
  
  .box {
    display: inline-block;
    background-color: white;
    border-radius: 3px;
    border-color: #E6E6E6;
    border-style: solid;
    border-width: 1px;
    min-width: 100%;
    height: 500px;
    text-align: left;
    margin-bottom: 30px;
  }
  
  .box-mobile {
    border: none;
    margin-bottom: 0;
  }
  
  .box .buttons{
    font-size: 2em;
  }
  
  .box .buttons .glyphicon{
    color: black;
    margin-right: 0.3em;
  }
  
  .box .padding {
    padding-left: 1.5em;
    padding-right: 1.5em;
    margin-bottom: 1.5em;
  }
  
  .box .image {
    margin-top: 5em;
    max-width: 100%;
    max-height: 100%;
    margin-bottom: 1em;
  }
  
  .box .hit {
    font-weight: bold;
    margin-bottom: 0.5em;
  }
  
  .grid {
    display: grid;
    grid-template: 1fr 1fr / 2fr 6fr 1fr 1fr;
  }
  
  .grid .grid-box {
    background-color: #FAFAFA;
    color: black;
    border-radius: 5px;
    border: 2px dodgerblue solid;
    grid-gap: 10px;
  }

  .grid .a {
    grid-row-start: 1;
    grid-row-end: 3;
  }
  
  .grid .b {
    grid-row-start: 1;
    grid-row-end: 2;
  }
  
  .grid .c {
    grid-row-start: 1;
    grid-row-end: 2;
  }
  
  .grid .d {
    grid-row-start: 1;
    grid-row-end: 2;
  }
  
  .grid .e {
    grid-row-start: 2;
    grid-row-end: 3;
    grid-column-start: 2;
    grid-column-end: 5;
  }

</style>